import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   images: {
    domains: ['res.cloudinary.com','assets.aceternity.com','i.pinimg.com','shopfootball.in' ], 
  }
};

export default nextConfig;
